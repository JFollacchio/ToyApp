<p id="notice"><%= notice %></p>

<h1>Microposttests</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @microposttests.each do |microposttest| %>
      <tr>
        <td><%= microposttest.content %></td>
        <td><%= microposttest.user_id %></td>
        <td><%= link_to 'Show', microposttest %></td>
        <td><%= link_to 'Edit', edit_microposttest_path(microposttest) %></td>
        <td><%= link_to 'Destroy', microposttest, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Microposttest', new_microposttest_path %>
